#!/bin/bash
# One-command launcher for SO-101 RL Training (SAC + HER) with Mac sleep prevention

FALLBACK_BIN="/Users/nam/miniforge3/envs/lerobot/bin/python"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "🚀 Starting SO-101 SAC + HER Reinforcement Learning Training..."
echo "☕ Caffeinate active: Mac display/sleep prevented during training."
exec caffeinate -dis "$FALLBACK_BIN" "${SCRIPT_DIR}/train_rl_ppo.py" "$@"

