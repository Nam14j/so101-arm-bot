#!/bin/bash
PYTHON_BIN="/Users/nam/miniforge3/envs/lerobot/bin/python"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec caffeinate -dis "$PYTHON_BIN" "${SCRIPT_DIR}/train_vision_ai.py" "$@"
